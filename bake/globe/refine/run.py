"""Refine stage driver (PLAN.md section 10.2).  STUB — replaced by the real implementation."""
from __future__ import annotations

from ..stubs import stub_refine

OUTPUTS = ["fine"]


def run(store, params, log=print) -> dict:
    return stub_refine(store, params, log)
