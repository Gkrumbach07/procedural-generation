"""Tiles stage (PLAN.md section 10.3).  STUB — replaced by the real implementation."""
from __future__ import annotations

from ..stubs import stub_tiles

OUTPUTS = ["tiles"]


def run(store, params, log=print) -> dict:
    return stub_tiles(store, params, log)
