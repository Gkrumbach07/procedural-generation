"""Derive stage driver (PLAN.md section 11).  STUB — replaced by the real implementation."""
from __future__ import annotations

from ..stubs import stub_derive

OUTPUTS = ["biome", "graph/rivers.json"]


def run(store, params, log=print) -> dict:
    return stub_derive(store, params, log)
